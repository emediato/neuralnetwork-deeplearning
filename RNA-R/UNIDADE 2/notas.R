# 21/09/22 UNIDADE 2
# o x vai ser mapeado no novo espaço

# camada intermdiaria:
#
# p(x) = SOMATORIO w_i * x_i
#
# X ---> H_p ---> Y^
#
#
# Z aleatorio funciona pous
#
# tomas cover, shenon - teoria da informação
# teorema de cover, um prob. nao linearmente separável :
# ao projetar um problema nao linear, projetamos em uma dimensao maior
# e poderemos ver onde está h
#
# consequencia p do grande: aumenta dimensao e pode gerar overfit
# aumentar p ao ponto de gerar um overfit. ao projetar em dimensao alta,
# deve-se tomar cuidado com o overfit!
#
#
# baseado em funcoes gaussianas
# as funcoes radiais permitem olhar as dimensoes dos dados,
# a dimensao no espaco de entrada possui varias entradas
#
# o criterio para selecionar
# pode chegar ao limite de uma funcao radial por amostra
# **truque de kernel: uma abordagem por amostra
#
# **RBF: mesmo principio do polinômio, projeçao fixa com apelo para estimar a funcao geradora dos dados
# ao fixar as funcoes, se obtem
#
# **SVM = "RBF", funcao gaussiana POR AMOSTRA e entao,
# faz-se um somátorio e uma abordagem diferente é feita
#
# ** ELM =
#
#
# o treinamento da camada intermediaria de redes RBF através de metodos de agrupamento de dados visa à ortogonalização das projeções dos agrupamentos no espaço da camada escondida
#  A ORTOGONALIZAÇÃO DOS AGRUPAMENTOS DE ENTRADA SEM QUE ISTO RESULTE EM UM MODELO SOBRE-AJUSTADO
#  DEPENDE DE PARÂMETROS A SEREM AJUSTADOS AD HOC  PELO USUÁRIO.
#
# .



